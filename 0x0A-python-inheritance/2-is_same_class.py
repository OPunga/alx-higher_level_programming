#!/usr/bin/python3
"""2-i_same"""


def is_same_class(obj, a_class):
    """if object is an istanace of a class
    args:
    object being
    class attribute
    """
    if type(obj) is a_class:
        return True
    else:
        return False
